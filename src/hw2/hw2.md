1. 
https://docs.google.com/spreadsheets/d/15LZF8e85s1Rjhu4FRCe7-iXkXLyHFGxceoxklw5tdlE/edit?usp=sharing

1) Существует проблема с Номер договора=Null, потому создаётся id (PK). (Иначе номер договора сгодится PK)


2) Зависимости связаные с Оплатой полной нету. Сумма оплат равна Авнасу - это верно не всегда (например, впроцессе совсем не ясно какая сумма договора).   

2.
Проблемы: <br>

1) В `Movie_Cast` - хранится `Last_Name` и `First_Name` и их обьеденение является первычным ключом, что может являться недостаточным для 'первичности' (Есть актера с одинаковыми ФИ). Гораздо лучше сделать первычным ключом `id_cast`. 

2) Также в `Movie_Cast` заменить `Title` на `id_movie` (соответсвенно для неё сделать табличку), что бы не хранить лишний  `varchar(256)` .

3) Тут присутствует проблема с расширением: при наличии нескольких режиссёров (например) уже нужно всё ломать, поэтому хочется вынести работников (не только cast) в отдельное таблицу и записывать их должности. (см. задание 3)

3.
https://dbdiagram.io/d/63457a82f0018a1c5fde3169




